#include <stdio.h>
/**
*main - contains a funtion puts that prints a given text
*Return: 0 (implies success in running the program)
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle);
return (0);
}
